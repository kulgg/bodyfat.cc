export default function robots() {
  return {
    rules: [
      {
        userAgent: "*",
      },
    ],
    sitemap: "https://bodyfat.kul.cx/sitemap.xml",
    host: "https://bodyfat.kul.cx",
  };
}
